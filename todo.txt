1. get rid of message, channel, embed,.. wrappers

2. get rid of bot class (everything from client)

3. separate class for permission checks

4. bot should not run without database

5. get rid of prefix, works through mentions, all commands available from one interface (bot only listens to mentions, replies, interactions and messages in it's threads)
    - every stage of interface should have buttons "back"(if not main menu), "help" and "delete"
    - only user who started the interface can interact (his id shown in embed's footer.text)
    - selecting a command should start an interface of that command (from there the command can be executed)
    - command should be executed in a new message
    - command's name should be shown in embed's author.name
    - help button should show help about the current stage of the interface (help about the command if currest stage is a command's interface)
    - back button should return you one stage back

6. update MySQL queries
